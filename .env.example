# Django Core Settings
SECRET_KEY=your-secret-key-here-change-in-production
DEBUG=True
ALLOWED_HOSTS=.localhost,.localhost.com,localhost.com,localhost,127.0.0.1

# Database Configuration
DATABASE_NAME=taskflow_db
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_TIMEZONE=Asia/Kolkata

# JWT Configuration
JWT_ACCESS_TOKEN_LIFETIME_MINUTES=60
JWT_REFRESH_TOKEN_LIFETIME_DAYS=1

# Multi-tenant Domain Configuration
BASE_DOMAIN=localhost

# Time Zone
TIME_ZONE=Asia/Kolkata

# REST Framework Throttling
THROTTLE_ANON_RATE=20/minute
THROTTLE_USER_RATE=100/minute


# REST Framework Pagination
PAGE_SIZE=20
PAGE_SIZE_QUERY_PARAM=page_size
MAX_PAGE_SIZE=100

# Django Q2 Configuration
Q_CLUSTER_NAME=TaskFlow
Q_WORKERS=2
Q_RECYCLE=500
Q_TIMEOUT=60
Q_RETRY=120
Q_QUEUE_LIMIT=50
Q_BULK=10
Q_ORM=default
Q_CATCH_UP=False
Q_MAX_ATTEMPTS=3


# Request Size Limits
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_NUMBER_FIELDS=1000


# AWS S3 Configuration
AWS_ACCESS_KEY_ID = 
AWS_SECRET_ACCESS_KEY = 
AWS_STORAGE_BUCKET_NAME = 
AWS_S3_REGION_NAME = 
AWS_S3_CUSTOM_DOMAIN =

# S3 File Access Configuration
AWS_DEFAULT_ACL = private  # Keep files private
AWS_S3_FILE_OVERWRITE = False  # Don't overwrite files with same name

# Presigned URL Configuration
AWS_PRESIGNED_URL_EXPIRATION = 3600

# File Upload Limits
MAX_UPLOAD_SIZE = 10485760 # 10 MB default

# Allowed file types: Images, Text, PDF, Word documents
ALLOWED_FILE_TYPES = image/jpeg,image/jpg,image/png,image/gif,image/webp,application/pdf,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document,text/plain

# CloudFront Configuration (for serving files via CDN)
AWS_CLOUDFRONT_DOMAIN =
USE_CLOUDFRONT = 